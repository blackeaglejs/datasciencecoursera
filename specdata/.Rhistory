## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,1:10)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
head(subset, n=5)
tail(subset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
head(subset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id, select=c(Date,pollutant,ID))
head(subset,n=5)
## Remove the missing values
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
head(subset,n=5)
## Remove the missing values
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantdata
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("cbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
print(filenames)
## pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id, select=c(Date, pollutant, ID))
tail(subset, n=5)
## Remove the missing values
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
gsubset <- complete.cases(Date, pollutant, ID)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
gsubset <- complete.cases(Date, pollutant, ID)
tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
gsubset <- complete.cases("Date", pollutant, ID)
tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
gsubset <- complete.cases("Date", pollutant, "ID")
tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
gsubset <- is.na(pollutant)
tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
gsubset <- is.na(pollutant)
print(gsubset)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
good <- is.NA(pollutant)
gsubset <- subset[good, ]
tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
good <- is.na(pollutant)
gsubset <- subset[good, ]
tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
print(pollutant)
## good <- is.na(pollutant)
## gsubset <- subset[good, ]
## tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
## Remove the missing values
good <- is.na($pollutant)
gsubset <- subset[good, ]
tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata",nitrate,3:8)
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
subset2 <- subset[$Date, pollutant, id]
tail(subset2, n=5)
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
subset2 <- subset[Date, id]
tail(subset2, n=5)
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
subset2 <- subset[,c(1,4)]
tail(subset2, n=5)
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
subset2 <- subset[,c(1,pollutant,4)]
tail(subset2, n=5)
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
subset <- subset(pollutantdata, ID == id)
subset2 <- subset[,c("Date",$pollutant,"ID")]
tail(subset2, n=5)
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id.
if(pollutant == "sulfate") {
subset1 <- subset(pollutantdata, ID == id)
subset2 <- subset1[ , c(1,2,4)]
tail(subset2, n=5)
}
else if(pollutant == "nitrate")
subset1 <- subset(pollutantdata, ID == id)
subset2 <- subset1[ ,c(1,3,4)]
tail(subset2, n=5)
end
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
## Select a subset of that data based on the id and pollutant.
if(pollutant == "sulfate") {
subset1 <- subset(pollutantdata, ID == id)
subset2 <- subset1[ , c(1,2,4)]
tail(subset2, n=5)
}
else if(pollutant == "nitrate") {
subset1 <- subset(pollutantdata, ID == id)
subset2 <- subset1[ ,c(1,3,4)]
tail(subset2, n=5)
}
end
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
names(pollutantdata)
## Select a subset of that data based on the id and pollutant.
if(pollutant == "sulfate") {
subset1 <- subset(pollutantdata, ID == id)
subset2 <- subset1[ , c(1,2,4)]
tail(subset2, n=5)
}
else if(pollutant == "nitrate") {
subset1 <- subset(pollutantdata, ID == id)
subset2 <- subset1[ ,c(1,3,4)]
tail(subset2, n=5)
}
end
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
names(pollutantdata)
## Select a subset of that data based on the id and pollutant.
##if(pollutant == "sulfate") {
##      subset1 <- subset(pollutantdata, ID == id)
##      subset2 <- subset1[ , c(1,2,4)]
##      tail(subset2, n=5)
##}
##else if(pollutant == "nitrate") {
##      subset1 <- subset(pollutantdata, ID == id)
##      subset2 <- subset1[ ,c(1,3,4)]
##      tail(subset2, n=5)
##}
##end
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
pollutantmean("/users/Zoheb2/dev/datasciencecoursera/specdata","nitrate",3:8)
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
names(pollutantdata)
## Select a subset of that data based on the id and pollutant.
if(pollutant == "sulfate") {
subset1 <- subset(pollutantdata, ID == id)
subset2 <- subset1[ , c(1,2,4)]
tail(subset2, n=5)
}
else if(pollutant == "nitrate") {
subset1 <- subset(pollutantdata, ID == id)
subset2 <- subset1[ ,c(1,3,4)]
tail(subset2, n=5)
}
end
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
pollutantmean <- function(directory, pollutant, id) {
## Load data into the dataset.
current_dir = getwd()
setwd(directory)
filenames <- list.files(path=directory)
pollutantdata <- do.call("rbind", lapply(filenames, read.csv, header = TRUE))
names(pollutantdata)
## Select a subset of that data based on the id and pollutant.
if(pollutant == "sulfate") {
subset1 <- subset(pollutantdata, ID == id)
subset2 <- subset1[ , c(1,2,4)]
tail(subset2, n=5)
}
else if(pollutant == "nitrate") {
subset1 <- subset(pollutantdata, ID == id)
subset2 <- subset1[ ,c(1,3,4)]
tail(subset2, n=5)
}
end
## Remove the missing values
##good <- is.na($pollutant)
##gsubset <- subset[good, ]
##tail(gsubset, n=5)
## Do a mean of the pollutant specified.
}
